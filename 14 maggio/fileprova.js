// Dichiarazione variabili
let nome = "Mario"; // string
const eta = 30; // number
let attivo = true; // boolean

// Tipi speciali
let valoreNonDefinito; // undefined
let valoreVuoto = null; // null

let messaggio = nome + " ha " + eta + " anni";

console.log(messaggio);

// Ci sono due dati di categorie: i tipi primitivi predefiniti nel sistema (string, number, boolean, undefined, null) e i tipi oggetti
// definiti dagli utenti (object, array, function).

//Entrambi sono predefiniti

//I secondi tipi di dati di categorie sono

//Java è case sensitive, quindi "nome" e "Nome" sono considerati due variabili diverse.

//Array e Object

//Array e oggetto (tipo object)
let numeri = [1, 2, 3]; // array

let persona = {
    nome: "Mario",
    eta: 30,
};